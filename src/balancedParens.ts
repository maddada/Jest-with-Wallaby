export default function balancedParens(n: number): boolean {
  // if (n < 1) throw new Error('Invalid Input: Negative Integer');
  // if (typeof n != '') throw new Error('Invalid Input: Not an Integer');
  return true;
}
